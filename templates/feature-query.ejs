import { createResolver } from '../../api/helpers'
import { <%- casing.pascalCase(name) %>Type, mapTo<%- casing.pascalCase(name) %>Type } from './types'

export const <%- casing.camelCase(name) %> = createResolver<<%- casing.pascalCase(name) %>Type>(async () => {
    return mapTo<%- casing.pascalCase(name) %>Type({})
})
